<div
  id="overlay"
  class="hidden fixed top-0 left-0 w-full h-full bg-gray-800 opacity-75 z-40"
></div>
<div
  id="modal"
  class="hidden fixed w-60 h-60 bg-gray-200 border-2 z-50 top-2/4 -translate-y-2/4 left-2/4 -translate-x-2/4"
>
  <form
    action="/post/<%= post.slug %>/delete"
    method="post"
    class="flex flex-col items-center text-center w-full"
  >
    <div class="w-full">
      <div>Are you sure?</div>
      this action <strong>cannot be undone</strong>
    </div>
    <button
      class="cursor-pointer border text-sm flex flex-col items-center justify-center p-2 w-2/4 rounded-lg bg-red-700 relative"
    >
      <i
        class="fa-solid fa-trash absolute text-red-400 -right-3 translate-x-full"
      ></i>
      <div>Delete the post</div>
    </button>
  </form>
</div>
